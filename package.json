{
  "name": "@solo-ist/gcp",
  "private": true,
  "type": "module",
  "pnpm": {
    "overrides": {
      "esbuild": ">=0.25.0"
    }
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
